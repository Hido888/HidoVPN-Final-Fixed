name: hidovpn
description: "HidoVPN - Professional VPN Client"
publish_to: 'none'

version: 2.5.0+7

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  flutter_v2ray_plus: ^1.1.3
  provider: ^6.1.2
  shared_preferences: ^2.3.3
  http: ^1.2.2
  flutter_animate: ^4.5.0
  percent_indicator: ^4.2.3
  iconsax: ^0.0.8
  url_launcher: ^6.3.1
  package_info_plus: ^8.1.3
  connectivity_plus: ^6.1.4
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_gen_runner: ^5.8.0

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/images/
